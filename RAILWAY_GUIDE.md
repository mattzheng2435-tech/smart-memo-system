# Railway 部署指南

## 📋 Railway 的作用

Railway 是一个云部署平台，可以为智能备忘录系统提供以下功能：

### 🚀 核心功能

1. **MCP Server 远程访问**
   - 部署后，Claude 可以通过 MCP 协议直接访问 Supabase 数据库
   - 任何使用 Claude 的地方都能查询和管理备忘录
   - 不局限于本地电脑

2. **Web API 接口**
   - 提供 REST API 管理备忘录
   - 支持手机 App、其他服务集成
   - 可通过 HTTP 请求创建/查询/完成任务

3. **Web 管理界面**
   - 浏览器直接访问管理界面
   - 可视化管理备忘录
   - 查看统计和报表

## 📊 功能对比

| 功能 | 当前方案 (无 Railway) | 部署 Railway 后 |
|------|----------------------|----------------|
| 创建/查询任务 | ✅ 本地脚本 | ✅ MCP/API |
| 每日自动邮件 | ✅ GitHub Actions | ✅ 同样 |
| 云数据库 | ✅ Supabase | ✅ 同样 |
| Claude 直接访问 | ❌ 仅本地 | ✅ 任何地方 |
| Web API | ❌ | ✅ |
| Web 界面 | ❌ | ✅ |
| 成本 | 免费 | $5/月起 (或免费额度) |

## 🎯 何时需要部署 Railway

### 需要部署的场景：

- ✓ 希望在任何设备上使用 Claude 访问备忘录
- ✓ 需要手机 App 管理备忘录
- ✓ 需要网页版管理界面
- ✓ 需要提供 API 给第三方服务
- ✓ 需要更好的可访问性

### 不需要部署的场景：

- ✓ 仅个人使用，通过本地脚本管理
- ✓ 每日邮件报告已满足需求
- ✓ 主要通过 GitHub 网页查看

## 📝 部署步骤（待执行时参考）

### 1. 安装 Railway CLI

```bash
npm install -g @railway/cli
```

### 2. 登录 Railway

```bash
railway login
```

### 3. 创建项目

```bash
cd mcp-server
railway init
```

### 4. 配置环境变量

在 Railway 控制台设置：
- `SUPABASE_URL`: https://lfhbowwktzoaeznfvxky.supabase.co
- `SUPABASE_SERVICE_KEY`: sb_secret_bl9B1bTRqU5vtjXV7lO6Yg_xO2YI308
- `DATABASE_URL`: (Supabase 数据库连接字符串)

### 5. 部署 MCP Server

```bash
railway up
```

### 6. 获取公共 URL

部署完成后，Railway 会提供一个公共 URL，例如：
`https://smart-memo-mcp.up.railway.app`

### 7. 配置 Claude Desktop

在 Claude Desktop 配置文件中添加：

```json
{
  "mcpServers": {
    "smart-memo": {
      "command": "node",
      "args": ["-e", "require('http').get('https://smart-memo-mcp.up.railway.app/sse', (r) => { r.pipe(process.stdout) })"]
    }
  }
}
```

## 💰 成本说明

- **免费额度**: 每月 $5 免费额度
- **基本套餐**: $5/月起
- **包含**: 512MB RAM, 0.5 vCPU, 1GB 存储

对于个人备忘录系统，免费额度完全够用。

## 📁 相关文件

- `mcp-server/` - MCP Server 代码
- `mcp-server/main.py` - 主程序
- `mcp-server/database/` - 数据库连接模块

## 🔗 有用的链接

- Railway 官网: https://railway.app/
- Railway 文档: https://docs.railway.app/
- Railway 控制台: https://railway.app/

## ⚙️ 当前系统架构

```
┌─────────────────┐
│  GitHub Actions │ (每天 09:30)
└────────┬────────┘
         │
         ├─► 生成报告 ──► Supabase 数据库
         │
         └─► 发送邮件 ──► Gmail ──► 工作邮箱

┌─────────────────┐
│ 本地 MCP Server │ (可选，本地运行)
└────────┬────────┘
         │
         └─► Claude 访问 ──► Supabase 数据库
```

## 🚀 部署 Railway 后的架构

```
┌─────────────────┐
│  GitHub Actions │ (每天 09:30)
└────────┬────────┘
         │
         ├─► 生成报告 ──► Supabase 数据库
         │
         └─► 发送邮件 ──► Gmail ──► 工作邮箱

┌─────────────────┐
│ Railway Cloud   │ (MCP Server)
└────────┬────────┘
         │
         ├─► Claude 访问 ──► Supabase 数据库
         │
         ├─► REST API ──► 手机/第三方
         │
         └─► Web 界面 ──► 浏览器
```

## 📌 备注

- 当前系统已完全满足日常使用需求
- Railway 是可选的扩展，不是必需的
- 可以随时根据需要部署
- 部署后不会影响现有功能

---

**创建时间**: 2026-01-22
**状态**: 待部署（按需）
**优先级**: 低
